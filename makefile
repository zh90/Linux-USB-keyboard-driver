##############################
+ P7 S, W: p: e  j* R

#usbkdb Makefile for linux

##############################

obj-m:=usbkbd.o

KERNELDIR ?= /lib/modules/$(shell uname -r)/build

PWD:=$(shell pwd)

default:
: a- \  n" @1 R! o; o; u6 B
$(MAKE) -C $(KERNELDIR) M=$(PWD) modules 
